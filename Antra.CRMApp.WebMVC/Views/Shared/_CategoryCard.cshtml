@model CategoryModel

<div class="col mb-3">
            <div class="card" style="width: 18rem;">
                <div class="card-body">
                    <h5 class="card-title">@Model.Name</h5>
                    <p class="card-text">@Model.Description</p>
           
                    <a asp-action="Edit" asp-controller="Category" asp-route-id="@Model.Id"  class="btn btn-primary">Edit</a>
                <a class="btn bg-danger" asp-action="Delete" asp-controller="Category" asp-route-id="@Model.Id" >Delete</a>
                <a class="btn bg-info" asp-action="Detail" asp-controller="Category" asp-route-id="@Model.Id" >Detail</a>
                </div>
            </div>
            </div>