@model VendorResponseModel

<div class="col mb-3">
            <div class="card" style="width: 18rem;">
                <div class="card-body">
                    <h5 class="card-title">@Model.Name</h5>
                    <p class="card-text">Email : @Model.EmailId</p>
                    <p class="card-text">Phone : @Model.Mobile</p>
                    <a asp-action="Edit" asp-controller="Vendor" asp-route-id="@Model.Id"  class="btn btn-primary">Edit</a>
                <a class="btn bg-danger" asp-action="Delete" asp-controller="Vendor" asp-route-id="@Model.Id" >Delete</a>
                </div>
            </div>
            </div>